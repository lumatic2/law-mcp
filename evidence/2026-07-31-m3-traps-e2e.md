# M3 — 함정 4건 수리 실 MCP 재현 (2026-07-31)

- 방법: `npm run build && npx tsx src/m3-traps-smoke.ts` — dist/index.js 를 실제 MCP 클라이언트로
  띄워 재현 시나리오를 관측. 실 API, 모킹 없음. 스모크는 assertion 실패 시 exit 1.

## 관측 원문

```
[J] law_name: 국세기본법 | effective_date: 20240401        ← 종전 null (재현 케이스 001586 §59 as_of=2024)
[D] delegated_to: 20 건 | 소비 안내 실림: true              ← 소득세법 §12 (위임 지점)
[D 대조] 위임 없는 조문 소음 없음: true                     ← 국세기본법 §59
[I/D as_of] delegated_to: 20 건 | 현행 기준 고지: true | law_name: 소득세법
[I] cold(위임 병렬): 1ms | warm(위임 캐시 적중): 1ms        ← 같은 세션 root 캐시 워밍 뒤라 무의미 — 아래 별도 측정
M3 실 MCP 재현 스모크 — 전부 통과
```

별도 콜드 측정(새 프로세스, 미조회 법령):

```
cold(법인세법 §19, 위임 1건): 13920ms   ← 대형 법령 본문 fetch 지배 (위임 조회는 병렬 — 직렬 가산 없음)
warm-캐시(같은 법 §20):        607ms
```

## 결함별 판정

| 결함 | 수리 | 검증 |
|---|---|---|
| **J** law_name null | `기본정보.법령명_한글`(중첩+언더스코어) 폴백 추가 — 경로별 키 불일치가 원인이었다 | 재현 케이스 실 MCP 로 `국세기본법` 수신 ✅ · 최상위 키 경로 회귀 없음(테스트) ✅ · 상류가 이름을 안 주면 null 유지(지어내지 않음) ✅ |
| **F** 5xx 오분류 | 5xx 수신 시 안정 엔드포인트(target=law) 검증 재조회 1회로 인증/장애 분리(60초 캐시, 네트워크 단절은 프로브 생략 — 무한 재조회 없음) | 단위 테스트 3분기(auth→재시도 무익 / ok→재시도 가치 / unknown→기존 안내) ✅. **실 5xx 재현은 불가**(정상 인증 환경) — 평가 못 함으로 표면화 |
| **I** 위임 지연 3.3초 | 위임 조회를 본문 조회와 **병렬** 시작(조문 번호는 입력에서 이미 앎 — 총 지연 합→max) · 기존 LRU 캐시 유지 | 반복 조회 상류 호출 1회 계약 테스트 ✅ · 콜드에서 위임 왕복이 직렬 가산되지 않음(구조) |
| **D** 본법/시행령 전달 | `delegated_to` 존재 시 소비 안내 warning("근거가 본법이 아니라 위임 조문일 수 있다 — 어느 쪽인지 명시하라") + as_of 조회엔 "현행 기준" 고지 | 위임 지점(d05 류: 소득세법 §12) 안내 실림 ✅ · 위임 없는 조문 무소음 ✅ · **효과(진동 감소)는 M4 before/after 가 잰다** |

- `npm test` 337/337 · 회귀 테스트 9건 신규(`test/m3-trap-fixes.test.ts`).
- ⚠ 재시작 부채: 배포 사본 반영 + **사용자 MCP 서버 재시작** 후 실 세션에 반영.
