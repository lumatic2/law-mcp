# M7 step-1 — 고정 비교 세트 불변성 + 범용축 재측정

- 일자: 2026-08-01 · milestone M7 · **확장 통합(184 레코드) 후** 기존 표본이 그대로인지 확인한다.
- 재현: `npx tsx bench/run.ts --cases evidence/bench/2026-08-01-m7-fixed/general-cases.json`

## 왜 결정적 축으로만 재는가 (계획 D2)

M4 의 `pass^3` 는 블라인드 3회 전승률이라 **1회 측정으로는 정의되지 않는다.** M5~M7 은 도구
코드(`src/`)를 바꾸지 않았으므로 에이전트 축 회귀의 실제 경로는 "코퍼스·러너 오염"뿐이고, 그것은
레코드 해시 대조와 표본 재현 검증으로 더 직접적으로 잡힌다. 없는 판정을 있는 척하지 않는다.

## ① 레코드 해시 불변 (에이전트 축 고정 43건)

`fixed43-hashes.json` — 키 순서 정규화 후 개별 직렬화한 해시. 파일 바이트는 60건 추가로 당연히
바뀌지만 **레코드는 변경 0건**. 통합 스크립트가 이 대조를 통과하지 못하면 중단하도록 만들었다.
`--cases tasks.json` 이 통합 후에도 정확히 43건을 선택한다.

## ② 범용축 재측정 (고정 57건) — M4 와 전 지표 동일

표본 재현 방법: 확장분(`provenance=expansion-2026-08-01`)을 제외한 dev·query 보유·기권 제외 =
**57건**으로, M4 `general-dev.json` 과 같은 표본이다(목록 = `general-cases.json`).

| 지표 | M4 (2026-07-31) | M7 (2026-08-01) | 판정 |
|---|---|---|---|
| **recall@3** | 84.21% | **84.21%** | 동일 |
| recall@1 | 63.16% | 63.16% | 동일 |
| 조문 정확도 | 75.0% (n=40) | 75.0% (n=40) | 동일 |
| 판례 hit율 | 68.42% | 68.42% | 동일 |
| 채점/에러 | 57 / 0 | 57 / 0 | 동일 |

소수점까지 동일하다 — 확장 통합이 기존 표본의 측정에 아무 영향을 주지 않았다.
중단점(회귀) 미발동.

## 측정 전 함정 점검 (Failure probe)

`dist-bench` 를 측정 직전 재빌드했다(M4 에서 2회 걸린 stale dist 함정).
- 빌드 시각 2026-08-01 01:53 · `tool-cli.js` sha256 `7c1b88ff32a84003…` ·
  `lawgo-provider.js` sha256 `01d2365254f74340…`
- 배포 사본(`custom-mcps/law-mcp`)은 `9ce7c24`(M4 수정 반영본)이고, M5·M6 는 평가 하네스만
  건드려 `src/` 무변경이므로 일치 상태다.
