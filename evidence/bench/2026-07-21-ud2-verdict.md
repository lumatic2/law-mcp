# UD2 종합 판정 — `aiSearch` 편입

- 일자: 2026-07-21 · 세트 `bench/golden-v2.json` dev 25건 (홀드아웃 미개봉)
- 원자료: `2026-07-21-ud2-ab.json` · `-run2-ud2-ab.json` · `2026-07-21-ud2-final.json`

## 판정: **채택**

| 지표 | LB5 (직전) | UD2 (현행) |
|---|---|---|
| 정답 포함률 recall@3 | 48.0% | **80.0%** |
| 1위 정확도 recall@1 | 32.0% | **72.0%** |
| 조문 출하율 | 0% (미출하) | **76.0%** |
| 조문 정확도 | 미측정(제품 경로에 값이 없었음) | **73.7%** (n=19) |
| 도메인 최저 | 행정 20% | 행정 60% |
| 검색 1회 지연(중앙) | 2958ms | **2939ms** |

채택 조건(F13) — **새로 깨지는 쿼리 0** + **순 이득 +8** (≥3). 2회 교차 측정 결과 동일.

recall@1 이 32% → 72% 로 뛴 것이 사실상의 사용자 체감이다. 1위가 맞는다는 것은 소비 LLM 이
목록을 고르지 않아도 된다는 뜻이다.

## 지연 — DoD 를 한 번 놓쳤고, 고쳐서 통과했다

첫 측정에서 중앙 **4079ms** 였다(DoD ≤3000ms 미달). 원인은 사다리와 `aiSearch` 를 **순차로**
돌린 것 — 두 채널은 서로의 결과를 안 쓰는데 굳이 기다리고 있었다. 동시 실행으로 바꾸니
**2939ms**, 대조군(2958ms)과 사실상 같다. **추가 채널의 지연 비용이 0 이 됐다.**

꼬리는 여전히 길다(최대 5369ms). 다만 대조군의 꼬리가 5590ms 로 더 길어, 이 꼬리는
`aiSearch` 가 아니라 기존 사다리의 것이다. 별도 finding 으로 남긴다(F19).

## 회귀

- **세무**: golden-v2 세무 5건 40% → 80%. 회귀 0.
- **전 도메인**: 어느 도메인도 내려가지 않았다(노동 40→80 · 형사 40→80 · 민사 100→100 ·
  행정 20→60 · 세무 40→80).
- **하류 도구**(UD0 교훈): `resolveLawId` 는 `aiSearch` 를 명시적으로 끄고 그 사실을 테스트로
  잠갔다. 순위를 바꾸는 변경이 `get_law_article` 을 오염시킨 전례가 있다.
- `npm test` 157/157.

## 남은 것

- 4건은 어느 배치로도 못 맞힌다: 육아휴직 신청 요건과 기간 · 범죄수익 몰수 추징 ·
  처분 사전통지 의무 · 과징금 부과 기준. 상류가 못 주는 것이라 랭킹으로 안 풀린다.
- 조문 미출하 6건 — `aiSearch` 가 그 법령을 안 짚었거나 법령 자체를 못 찾은 경우다.
- **홀드아웃은 열지 않았다.** horizon close 시 1회(코드로 강제 — UD1 step-2).
