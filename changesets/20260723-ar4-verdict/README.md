# AR4 — 홀드아웃 개봉 판정 + horizon 종료 대조

- 일자: 2026-07-23 · milestone: AR4 · 갈래: tooling · **`src/` 0줄**
- 증거: `evidence/bench/2026-07-23-ar4-holdout.md` (+ `-holdout/`·`2026-07-23-ar3-ab/`)

## 판정

- **닫는 기준 6/6 충족.**
- 홀드아웃 10건 × 5회: `pass^5` **100%**, `SR@1` 98%. 과적합 **기각**.
- ⚠ 그러나 **변별력이 낮다** — 홀드아웃 6/10 이 내가 주제까지 고른 케이스라
  도구가 잘 다루는 영역에서 문제를 낸 셈이다(프리모템 ① 발화, 문자열이 아니라 주제 경로로).
  **절대 수치는 신뢰하지 않는다.**
- **AR3 재판정: `SR@1` 순 이득 미달.** ON 80% vs OFF 88.3% (−8.3%p), `pass@3` 는 +5%p.
  경고 정밀도 75% 라 성공한 검색 넷 중 하나에 재질의를 시킨다. 유지 여부는 제품 판단.

## 변경

- `bench/tool-cli.ts` — `--no-vocab-gap` (A/B 대조군 전용)
- `bench/agentic-baseline.ts` — `--holdout` 채점 경로 · `queries: []` 를 0턴으로 받던 결함 수정
  (대조군 `SR@1` 이 부풀던 원인)

## 검증

- 봉인 양방향: 개봉 **전**·**후** 모두 플래그 없이 거부
- 유출 34건 0 · 채점기 9/9 · `npm test` 314/314 · `git diff --stat src/` 0줄
- 실 MCP 표면 E2E 1건 통과(재시작 반영 확인). 결함 1건(`as_of` 가 법령ID 미지원) finding 큐로.
