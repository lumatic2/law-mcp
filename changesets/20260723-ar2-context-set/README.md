# AR2 step-1·2 — 맥락 세트 + 유출 탐지기 + 홀드아웃 봉인

- 일자: 2026-07-23 · milestone: AR2 · 갈래: tooling

## 산출물

- `bench/leak-detect.ts` — 맥락 문단에 정답 법령명·조문번호가 있으면 **exit 1**.
  어간(`국세기본법`→`국세기본`)·공백 끼움·조문 표기 변형까지 막는다.
- `bench/golden-tax-agentic.json` — dev 24건(구 홀드아웃 20 + 기권 4) · holdout 10건.
- `bench/agentic-set.ts` — 읽는 경로를 하나로 모아 봉인을 우회 못 하게 한다.
  봉인은 `bench/run.ts` 의 `assertHoldoutSeal` **재사용**(구현이 둘이면 하나로 샌다).

## 검증

- 유출 탐지기: 34건 전부 통과. **일부러 유출시킨 문단은 6건 적발 + exit 1**(실패 프로브).
- 봉인 양방향: 플래그 없이 holdout → 거부 / 플래그 있으면 10건 열림.
- 정답 라벨 10건 전부 실 API 조회로 존재 확인.
- `npm test` 314/314 · `tsc` exit 0 · `git diff --stat src/` **0 줄**

## 설계 메모

- 맥락 문단 페르소나 = **법령명도 조문번호도 모르는 사람**. 과하면 낙관 왜곡, 빈약하면
  비관 왜곡이라 기준을 "실제 세무 대화에서 사람이 할 법한 말"로 잡고 애매하면 버렸다.
- 기권 케이스는 **자연스럽게 답이 없는 것**으로 만들었다(폐지 세목·미입법). 티나는 함정은
  실제 분포를 대표하지 못해 기권 정밀도를 왜곡한다.
