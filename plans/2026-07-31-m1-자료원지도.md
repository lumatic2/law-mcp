# PLAN — M1 · 자료원 지도 확정 + before 기준선

> 생성: 2026-07-31 · 산출물: changeset(tooling) · scope: 상류 프로브·처분표 ADR·재기준선 측정
> 소비 리서치: `research/2026-07-31-세법완성-자료원지도.md`

Status: approved (2026-07-31 사용자 승인 — M1→M2·M3→M4 연쇄 일괄)
- execution mode: continuous

## 위계
- **북극성**: 한국 사람들이 '법' 작업을 AI 에이전트로 할 때 설치하는 MCP 의 대표 중 하나가 된다 (← `CLAUDE.md` 「북극성」)
- **목표(연쇄)**: 세법 완성 — 닫는 기준 4축(자료원 완결·측정 재기준선·함정 소진·실전 리허설, 2026-07-31 사용자 확정). 연쇄 = M1→M2·M3→M4.
- **milestone**: M1 — 세무 근거 유형 전수에 대해 "경로 있음/없음+이유"가 판정된 처분표를 만들고,
  도구 변경 전 기준선을 측정해 M4 의 before 로 남긴다.
  milestone 규모 근거: 독립 step 2개(상류 프로브·처분 / 기준선 측정) + 통합 검증(처분표가
  근거 유형 지도 전 행을 덮고, 기준선이 M4 비교 가능 형식으로 남음).

## 왜
- 리서치 결과 "심판례·예규 미연결"은 낡은 전제였고, 진짜 공백(기본통칙·집행기준·조세조약 등)은
  **상류 경로 존재 자체가 미확인**이다. 확인 없이 연결 계획을 짜면 TV2 이전의 어순 오판
  (문서상 `specialDeccTt` → 실제 `ttSpecialDecc`)을 반복한다.
- 코퍼스가 94topic 인데 유효 기준선은 AR2 dev 20건뿐. M2·M3 가 도구를 바꾸기 **전에** 찍어야
  before/after 가 성립한다.

## run 전 scope 결정 (확정)
- **결정**: `research/2026-07-31-세법완성-자료원지도.md` §③ 미확인 8건 전수 프로브 + 처분표 ADR +
  dev 기준선(범용·에이전트 하네스) 측정까지.
- **non-goals**: 자료원 연결 구현(M2) · 결함 수리(M3) · holdout 사용(봉인 유지) · 스크래핑 구현 ·
  회계·타 분야·npm/배포.
- **중단점(stop points)**: OC 인증 문제로 프로브 자체가 불가하면 blocked / 프로브 결과가 처분
  기준(결정 로그 D1)으로 판정 불가능한 새 유형을 드러내면 decision_required / 시크릿·약관 위반
  소지가 있는 접근이 필요해지면 risk_gate 로 정지.
- **롤백/정리**: `src/` 무변경(조사+측정+문서만). 산출물은 research·evidence·ADR 파일이라
  되돌리면 파일 삭제로 끝난다.

## 스캐폴딩 결정
- source-of-truth: 처분표 정본은 `docs/adr/`(번호 승계) — research 문서는 재료, ADR 이 판정.
  기준선 정본은 `evidence/bench/2026-07-31-m1-baseline/`.
- 검증: 프로브는 실 API 응답 원문 첨부(요약만 금지) · 기준선은 러너 산출 JSON + 지표 표.
- 배포/운영: 해당 없음 — `src/` 무변경이라 배포 사본·재시작 부채 없음.
- 검토 후 제외: frontend·design·data 스키마 — 조사·측정 milestone 이라 해당 없음.

## 결정 로그
- D1 — M2 연결 처분 기준: **공식 API 가 있는 것만 연결한다.** NTS(taxlaw.nts.go.kr) 등 API 미확인
  표면의 스크래핑은 하지 않는다(약관 미확인·유지비) — API 없음이 확인되면 "공백+이유"로 기록하고
  실무 영향은 M4 리허설이 잰다.
  - status: resolved (2026-07-31 승인 제시에 포함해 사용자 확정)
- D2 — holdout 봉인 유지: 기준선·재측정 전부 dev 79건만 쓴다. holdout 45건은 소진 상태 그대로 둔다.
  - status: resolved (ADR 0002 기존 규약 준수)
- status: resolved

## Step 트리
- [x] **step-1 — 상류 프로브 + 처분표 ADR**
  - Artifact: `research/2026-07-31-m1-상류프로브.md`(실 응답 원문) + `docs/adr/` 처분표 ADR
    (근거 유형 14행 × 판정: 연결됨/연결 대상/공백+이유)
  - Files: read `research/2026-07-31-세법완성-자료원지도.md`·`src/providers/source-adapter.ts`·
    `research/2026-07-21-tax-vertical-upstream-probe.md` / write `research/2026-07-31-m1-상류프로브.md`·`docs/adr/` 신규 ADR.
    프로브 수단: 미등록 target 은 raw 법제처 API 를 scratchpad 스크립트로 직접 호출(`LAW_API_OC` 재사용,
    기존 `fetchLawApi` 파라미터 규약 준수) — MCP 도구를 거치지 않는다
  - Dependencies: 없음
  - Risk: 기계적 (읽기 전용 API 프로브)
  - Verify: 미확인 8건(기본통칙/집행기준 경로 · `trty` · `lsRlt`/`couseLs`/`drlaw` 어순 재확인 ·
    `expc` 의 NTS 유형 커버리지 · `ntsExpc` 원문링크 도달 · `ttDecc` 데이터기준일 lag ·
    국세청 고시 `admrul` 도달 · `oldAndNew`/`licbyl`/`lsStmd`/`dlytrm` 재평가)에 전부 판정이 있고
    각 판정에 실 API 응답 증거가 붙어 있다 · ADR 이 근거 유형 지도 14행을 전부 덮는다
  - Failure probe: 빈 응답 target 은 기관약어+서비스명 정순·역순 양쪽을 시도한 뒤에만
    "없음" 판정한다(어순 오판 재발 방지) · 프로브 중 5xx 를 만나면 "일시 장애"로 넘기지 않고
    OC 유효성을 별도 재조회로 확인한다(결함 F 함정 회피)
  - Commit: `research(m1): 상류 프로브 전수 + 자료원 처분표 ADR`
- [ ] **step-2 — before 재기준선 측정**
  - Artifact: `evidence/bench/2026-07-31-m1-baseline/` — 범용 러너 dev 결과 JSON + 에이전트 하네스
    실행 결과(블라인드 세션별 로그) + 지표 표(recall@3 · pass^3 · SR@1)
  - Files: read `bench/run.ts`·`bench/tool-cli.ts`·`evidence/bench/2026-07-23-tf2-baseline/BRIEF.md`·
    `evidence/bench/2026-07-23-tf2-baseline/tasks.json`·`evidence/bench/2026-07-23-ar2-baseline.md`(재현 절차) /
    write `evidence/bench/2026-07-31-m1-baseline/`
  - Dependencies: 없음
  - Risk: 위험 (실 API 대량 호출 — 상류 부하·시간)
  - Verify: `npm run bench:golden -- --split dev` 가 exit 0 으로 완주하고 결과 JSON 이 저장됨 ·
    에이전트 지표는 **AR2 와 동일 프로토콜** — 블라인드 세션 **3회 독립 실행**(orca 스폰 신규 세션,
    `bench/tool-cli.ts` 만 사용, `bench/corpus.json`·`evidence/`·`plans/` 열람 금지) 후 스코어러로
    `pass^3`·`SR@1` 산출(n=1 실행으로 pass^3 를 산출하지 않는다) · 지표 표가 AR2
    기준선(dev 20건 pass^3 90% · SR@1 80%)과 나란히 비교 가능한 형식임
  - Failure probe: 러너 중도 실패(상류 타임아웃 등) 시 부분 결과를 기준선으로 채택하지 않는다 —
    재실행하거나 실패 케이스 목록을 명시하고 partial 로 기록
  - Commit: `bench(m1): 도구 변경 전 dev 재기준선 측정`

## 검증/DoD
- 근거 유형 지도 14행 전부에 처분 판정(연결됨/연결 대상/공백+이유)이 ADR 로 존재한다.
- 미확인 8건이 실 API 증거와 함께 소진된다 — "미확인" 잔존 = M1 미완료.
- dev 기준선(범용+에이전트)이 evidence 로 남고 M4 가 같은 세트로 재측정해 비교할 수 있다.
- `git diff --stat src/` 0줄 · `npm test` 전건(무변경 확인용).

## finding 큐
- (실행 중 발견분 append)

## 진행 로그 (append-only)
- 2026-07-31 plan 작성.
- 2026-07-31 step-1 완료 — 프로브 `research/2026-07-31-m1-상류프로브.md` · 처분표 ADR 0003.
  주요 반전: `trty` 개통 147건 · `lsRlt` 재개봉 · 예규 본문 NTS 문서 API 도달 · 심판례 신선도 기각.
