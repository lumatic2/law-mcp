# PLAN — M4 · after 측정 + 실전 리허설 + 완성 판정

> 생성: 2026-07-31 · 산출물: changeset(tooling)+보고서 · scope: before/after 재측정 · 실전 리허설 ·
> 세법 완성 4축 대조 보고
> 소비 리서치: `research/2026-07-31-세법완성-자료원지도.md` · M1 기준선 · M2/M3 결과

Status: approved (2026-07-31 사용자 승인 — M1→M2·M3→M4 연쇄 일괄)
- execution mode: continuous

## 위계
- **북극성**: 한국 사람들이 '법' 작업을 AI 에이전트로 할 때 설치하는 MCP 의 대표 중 하나가 된다 (← `CLAUDE.md` 「북극성」)
- **목표(연쇄)**: 세법 완성 — 측정·리허설 축, 연쇄의 닫는 milestone. 연쇄 = M1→M2·M3→**M4**.
- **milestone**: M4 — M1 기준선과 같은 세트로 재측정해 before/after 를 남기고, 실 세무 시나리오
  리허설로 "우회 지식 없는 도달"을 확인한 뒤, 닫는 기준 4축 대조 보고서를 낸다.
  milestone 규모 근거: 독립 step 3개(재측정 / 리허설 / 판정 보고) + 통합 검증(4축 전부가
  증거로 연결된 보고서).

## 왜
"완성됐다"가 주장으로 남지 않으려면: 같은 자로 두 번 재고(before/after), 실제 사용 형태로
확인하고(리허설), 판정을 증거에 물려야 한다(보고서). 닫는 판정 자체는 사용자 소유 —
이 milestone 은 판정 재료를 완성한다.

## run 전 scope 결정 (확정)
- **결정**: dev 세트 재측정 + 리허설 5건 + 4축 대조 보고서. 여기까지가 연쇄의 끝 —
  "다음 분야(②)로 넘어갈지"는 보고서를 본 사용자가 결정한다.
- **non-goals**: holdout 사용 · 새 기능 구현 · 코퍼스 문제 추가(후보 L 소관) · npm/배포 판단
  (§진행 순서 ③ — 사용자 발화 전 제안 금지).
- **중단점(stop points)**: after 지표가 before 보다 유의하게 나쁘면(회귀) blocked 로 정지하고
  원인 분석을 제시 / 리허설이 M2 처분(공백 유지) 탓의 도달 실패를 드러내면 decision_required
  (공백 재판정 여부는 사용자 결정).
- **롤백/정리**: `src/` 무변경(측정·문서만). evidence·보고서 파일 삭제로 원상 복귀.

## 스캐폴딩 결정
- source-of-truth: before = `evidence/bench/2026-07-31-m1-baseline/` (M1 산출) ·
  after = `evidence/bench/` 신규 디렉터리 · 판정 보고서 = `archive/reports/`(git 추적 확인).
- 검증: 재측정은 M1 과 동일 러너·동일 dev 세트·동일 지표(다른 세트로 재면 비교 무효) ·
  리허설은 실 MCP 클라이언트 세션 원문 기록.
- 배포/운영: 측정 전에 M2·M3 의 변경이 배포 사본에 반영·재시작돼 있어야 한다(선행 조건 —
  재시작 부채가 남아 있으면 측정을 시작하지 않는다).
- 검토 후 제외: frontend·design·data 스키마 — 측정·문서 milestone 이라 해당 없음.

## 결정 로그
- D1 — 리허설 시나리오 5건(대표성 기준: 코퍼스 세법 type 분포 상위 + 신규 연결 자료원 검증):
  ① 양도소득세 신고 근거 찾기(공제·비과세 요건, 귀속연도 `as_of`) ② 가산세 불복 — 심판례·예규
  선례 체인 ③ 법인세 손금 인정 — 본법→시행령 위임 사슬 추적 ④ 국제거래 — 조세조약과 국내법
  교차(M2 신규 연결 검증) ⑤ 기본통칙·집행기준이 근거인 질문(M2 공백 처분 시 실무 영향 실측).
  각 건은 "세무 배경지식 없는 에이전트가 도구만으로 근거에 도달하는가"로 채점.
  **블라인드 보장**: 리허설 수행 세션에는 위 성격 라벨("M2 신규 연결 검증" 등)을 제거한 순수
  질문문만 전달한다 — 이 계획 문서·레포를 읽은 세션이 직접 수행하면 무효이므로 orca 로 별도
  세션을 스폰해 격리한다.
  - status: resolved (2026-07-31 승인 제시에 포함해 사용자 확정)
- D2 — "유의한 회귀" 판정선: 재측정 `pass^3` 가 M1 before 대비 5%p 초과 하락이면 회귀로 본다.
  근거는 선례 수치가 아니라 **운용상 채택**(dev 79건에서 5%p ≈ 4건, 케이스 플레이키니스 1~2건을
  노이즈로 허용하는 폭) — AR2 라운드 간 집계 변동폭이라는 수치는 존재하지 않는다.
  - status: resolved
- status: resolved

## Step 트리
- [x] **step-1 — after 재측정**
  - Artifact: `evidence/bench/` after 결과 + before/after 비교표(지표별 Δ)
  - Files: read `evidence/bench/2026-07-31-m1-baseline/`·`bench/run.ts` / write `evidence/bench/` 신규
  - Dependencies: 없음
  - 착수 전제(milestone 순서는 ROADMAP marker 소유): M2·M3 완료+배포 사본 반영+재시작 —
    미충족이면 blocked, Failure probe 가 이를 검사한다
  - Risk: 위험 (실 API 대량 호출)
  - Verify: M1 과 동일 dev 세트·동일 지표로 산출 완료 · 비교표에 전 지표 Δ 명시 · 회귀 판정선
    (D2) 통과
  - Failure probe: 측정 시작 전 배포 사본 dist 해시·서버 재시작 여부를 확인한다 — 소스만 바뀐
    상태의 측정은 무효(배포 사본 분리 함정)
  - Commit: `bench(m4): after 재측정 + before/after 비교`
- [x] **step-2 — 실전 리허설 5건**
  - Artifact: `evidence/2026-07-31-m4-rehearsal.md` — 시나리오 5건(결정 로그 D1)의 실 MCP 세션
    기록(도구 호출 체인·도달 여부·막힌 지점 원문). 수행은 orca 스폰 블라인드 세션(D1 블라인드
    보장 절차) — 오케스트레이터는 순수 질문문 전달과 채점만 한다
  - Files: read M2/M3 changeset README·`bench/tool-cli.ts` / write `evidence/2026-07-31-m4-rehearsal.md`
  - Dependencies: step-1
  - Risk: 기계적 (측정·기록 작업)
  - Verify: 5건 전부 수행 기록 존재 · 각 건에 도달/부분도달/실패 판정과 근거(응답 원문) ·
    실패 건은 원인 분류(어휘 공백/자료원 공백/도구 결함)
  - Failure probe: 리허설 수행 에이전트에 레포·코퍼스 내용을 노출하지 않는다(블라인드) —
    노출되면 해당 건은 무효로 재수행
  - Commit: `evidence(m4): 실전 리허설 5건 기록`
- [ ] **step-3 — 세법 완성 판정 보고서**
  - Artifact: `archive/reports/` 판정 보고서 — 닫는 기준 4축(자료원 완결/측정/함정 소진/리허설)
    × 증거 링크 대조표 + 미충족 항목의 잔여 목록 + CANDIDATES 재적재
  - Files: read M1~M4 전 evidence·처분표 ADR / write `archive/reports/` 신규·`plans/horizons/CANDIDATES.md`(발견 적재)
  - Dependencies: step-2
  - Risk: 없음
  - Verify: 4축 전부 "충족/미충족+증거 링크" 로 판정돼 있고 링크가 실존 파일을 가리킴 ·
    보고서 경로가 git 추적됨(`report_close` 내구성 검사 통과)
  - Failure probe: 미충족 축을 "대체로 충족"으로 뭉개지 않는다 — 부분 충족은 부분으로 기록하고
    잔여를 CANDIDATES 에 적재했는지 확인
  - Commit: `report(m4): 세법 완성 4축 판정 보고`

## 검증/DoD
- before/after 비교표가 동일 세트·동일 지표로 존재하고 회귀 판정선을 통과한다.
- 리허설 5건의 실 세션 기록이 evidence 로 남는다.
- 4축 대조 판정 보고서가 git 추적 경로에 존재한다. **닫는 판정(다음 분야 진행 여부)은
  보고서를 본 사용자가 내린다** — 에이전트가 "완성" 을 선언하지 않는다.
- `git diff --stat src/` 0줄 · `npm test` 전건.

## finding 큐
- (실행 중 발견분 append)
- 2026-07-31 step-1 1라운드 회귀: pass^3 86.0→79.1%(−6.9%p, 판정선 초과). 원인 = M3 step-4 의
  DELEGATION_NOTICE 1차 문구가 ADR 0001 판별 기준 없이 시행령 쪽으로 편향(tax-46 본법 라벨
  케이스 뒤집힘 + tax-04 시행령 라벨 케이스 개선의 양면 패턴이 증거). 문구를 ADR 0001 로 정렬
  후 2라운드 재측정. `evidence/bench/2026-07-31-m4-after/round1-regressed/`.
- 2026-07-31 오퍼레이터 실수: 1라운드 run1~3.json 을 보존 전에 삭제 — 요약·케이스 diff 만
  재구성 보존. 다음부터 회귀 라운드 원자료는 즉시 커밋 후 재측정.
- 2026-07-31 step-2 운영 사고: orca 스폰 프롬프트가 Enter 미전송으로 45분 대기 방치 —
  스폰 후 화면 read 로 실행 시작을 확인해야 한다(전송 성공 ≠ 실행 시작).
- 2026-07-31 step-2 발견(r4): trty 검색 정규화 결함 — "미국"→"미합중국" 동의어 미확장 +
  조약명 띄어쓰기("이중과세 회피") 민감 매칭. 한·미 조세조약은 색인에 존재("미합중국
  이중과세" 2건 히트)하나 일상 표기로는 미도달. CANDIDATES 적재 대상(신규).
- 2026-07-31 step-2 발견(r2): ntsExpc 주제 질의가 전문검색 폴백 시 관련도 무관 결과 —
  검색 표면 품질 후보. r5 는 ADR 0003 처분(기본통칙 공백 유지)의 실무 영향 실측 —
  plan 중단점 조건 해당, 공백 재판정은 사용자 결정으로 step-3 보고서에서 제기.

## 진행 로그 (append-only)
- 2026-07-31 plan 작성.
- 2026-07-31 step-1 완료(2라운드) — 1R 회귀(pass^3 −6.9%p) → DELEGATION_NOTICE ADR 0001 정렬 →
  2R pass^3 86.0%(Δ0, 판정선 통과)·pass@3 +7.0%p·전패 4건 중 2건 완전 구제.
  `evidence/bench/2026-07-31-m4-after/SUMMARY.md`.
- 2026-07-31 step-2 완료 — 블라인드 리허설 5건: 도달 2 · 부분도달 3 · 실패 0 · 추측 답변 0.
  미도달 원인 = 도구 결함 2(r2 ntsExpc 관련도 / r4 trty 정규화) + 자료원 공백 1(r5 기본통칙,
  ADR 0003 처분). `evidence/2026-07-31-m4-rehearsal.md`.
