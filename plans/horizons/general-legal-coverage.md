# HORIZON — 범용 법률 커버리지 (general-legal-coverage)

> 생성: 2026-07-20 · ROADMAP marker: `harness:goal id="general-legal-coverage"` · 상태: active
> cascade 중간(Horizon). 위계: Objective(`OBJECTIVE.md`) → **Horizon**(이 문서) → Milestone(`plans/<date>-<milestone-id>-<slug>.md`) → Step.
> 진행 상태의 정본은 `ROADMAP.md` marker — 이 문서의 체크박스는 milestone boundary 에서만 동기화.

## 목표

세무 특화 도구를 **법 일반 도구**로 바꾼다. Objective 3축을 동시에 민다 — 넓이(비세무 도메인에서
정답에 닿는가) · 커버리지(법령·판례·행정규칙 외 법원(法源)) · 도달(어느 법 **몇 조**인가) — 그리고
그 품질이 선 뒤 마지막으로 설치 가능성(누구나 붙일 수 있는가)을 연다.

## 왜 지금 (실측이 드러낸 갭)

리서치 `research/2026-07-20-general-legal-coverage-probe.md` 실측:

- **넓이**: 비세무 13쿼리에서 hit율은 92%인데 **정답 법령 상위 3 포함률은 31%**. "정당방위 성립
  요건" → 민법·관세법·전파법, "음주운전 처벌 기준" → 병역법·선원법·보험업법. hit율이 착시였다.
- 원인 규명: 이 도구는 세무 특화가 아니라 **희귀어 특화**였다. 본문검색이 전 법령 대상이고 관련도
  랭킹이 없어(ib3 확인), 일반 법률어(해제·시효·요건)는 노이즈에 묻힌다.
- **커버리지**: 법령해석례·헌재결정례·행정심판재결례·자치법규·법령용어 5종의 DRF 타깃이 **전부 실재**
  하고 응답 구조도 기존과 동형인데 미지원.
- **도달**: 조문 단위 검색 타깃은 upstream 에 **없음**(4종 후보 전부 빈 응답). 다만 법령 전문 응답에
  조문 구조가 들어 있어 **클라이언트에서 구성 가능**하다.
- 직전 horizon(issueback-repair)은 어스회계법인 실소비 결함만 입력으로 삼았다 — 비세무 도메인은
  한 번도 측정된 적이 없다. Objective 를 세운 지금이 그 사각을 여는 시점이다.

## 무감독 분량

**승인 후 최소 3 무감독 세션.** 근거: milestone 4개 / 예상 changeset 합계 ≥10. LB1 은 측정 하네스
(골든셋·러너·기준선), LB2 는 조문 인덱스·검색·도구 노출로 각 3 changeset 급, LB3 은 법원 5종
어댑터, LB4 는 배포·문서. LB1→LB2 는 의존(측정 없이는 개선 주장 불가)이라 연쇄가 자연스럽다.

## 담을 milestone (계획 — active 는 ROADMAP 이 정본)

| # | ID | 제목 | 왜 milestone 규모인가 | plan doc | 승인 | 소비 리서치 |
|---|---|---|---|---|---|---|
| 1 | **LB1** | 정답 도달 측정 하네스 | 골든셋 데이터 + 러너 스크립트 + 기준선 리포트 = 독립 changeset 3, 통합검증=기준선 수치 산출 | `plans/2026-07-20-lb1-answer-reach-harness.md` | 대기 | probe §1 |
| 2 | **LB2** | 조문 단위 도달 + 랭킹 실질 개선 | 조문 인덱스 구성 + 조문 검색 도구 + 검색 랭킹 반영 = 독립 changeset 3, 통합검증=LB1 골든셋 재측정 | `plans/2026-07-20-lb2-article-level-reach.md` | **정지(step-2 기각, 방향 재선택 대기)** | probe §3 + prior-art |
| 3 | **LB3** | 누락 법원 5종 도구화 | 법령해석례·헌재결정례·행정심판재결례·자치법규·법령용어 어댑터 + 도구 노출 = 독립 changeset 3+, 통합검증=법원별 실 API 도달 | `plans/2026-07-20-lb3-missing-sources.md` | 대기 | probe §2 |
| 4 | **LB4** | 설치 가능성 (배포·발견성) | 공개 범위·배포 채널이 **사용자 소유 결정** — 착수 시점에 plan 을 펼친다 | (LB4 착수 시 작성) | 보류 | probe §4 + 레지스트리 조사(미실시) |

## 닫는 기준 (선언 — close 시 실측 대조)

| # | 기준 | 관측 방법 |
|---|---|---|
| 1 | 골든셋 정답 포함률(top-3) **31% → ≥70%** | `npm run bench:golden` 출력 수치 |
| 2 | 조문 단위 질의 정답률 — 골든셋 중 조문 라벨이 있는 항목에서 **≥70%** 정확한 조문 번호 도달 | 같은 러너의 조문 축 출력 |
| 3 | 법원 5종이 MCP 도구로 노출 + 각 **1건 이상 실 API 도달** | 재시작된 MCP 로 도구별 호출 로그 |
| 4 | 회귀 없음 — 기존 세무 쿼리 스위트(ib1~ib3 재현 6종) 전부 무변 | `npm test` + 실 API 재현 스크립트 |
| 5 | LB4 포함 시: 외부인이 README 만으로 설치 가능 | 빈 환경에서 설치 절차 1회 재현 |

## 미리 쓰는 실패 회고 (프리모템)

이 horizon 이 끝났는데 실패했다. 왜?

1. **"랭킹을 고쳤다"고 주장했지만 골든셋에만 과적합됐다.** 13개 쿼리를 보고 튜닝해 수치는 올랐는데
   새 쿼리에선 그대로. → 예방: 골든셋을 **개발셋/홀드아웃으로 분리**하고(LB1 DoD), 홀드아웃은
   LB2 완료 시점에 단 한 번만 측정한다. 튜닝 중 홀드아웃 열람 금지를 plan 에 명시.
2. **조문 인덱스가 비용 폭탄이 된다.** 후보 법령마다 전문(300~600KB)을 받다 보니 검색 1회가
   수 초·수 MB가 되어 실사용 불가. → 예방: LB2 step 에 **비용 예산을 사전 선언**(검색 1회당 추가
   fetch ≤5건, 캐시 필수, 실패 시 기존 경로로 graceful degrade)하고 DoD 에 지연시간 측정을 넣는다.
3. **법원 5종을 붙였는데 아무도 안 쓴다 — 도구 수만 늘어 선택 부담이 커진다.** → 예방: LB3 의 DoD 를
   "도구 추가"가 아니라 **"골든셋 쿼리 중 N건이 새 법원에서 정답을 얻는다"**로 잡는다. 기여를
   못 보이는 법원은 붙이지 않고 기록만 남긴다.

## Objective 임팩트 (close 시 기록 — §B3 완료 hook 5)

- (close 시 작성)

## 링크

- 위(Objective): `OBJECTIVE.md`
- 리서치: `research/2026-07-20-general-legal-coverage-probe.md` · `research/2026-07-21-lb2-prior-art-candidate-generation.md`
- 아래(Milestone plans): 위 번들 인덱스 표 참조
