# PLAN — TF1 단일 코퍼스

> 생성: 2026-07-23 · 산출물: changeset(tooling) · scope: 골드 4파일 → `bench/corpus.json` 통합 + 수치 재현

Status: approved (2026-07-23 사용자 승인 — horizon 전체 연쇄)
- execution mode: continuous

## 위계
- **북극성**: 한국 사람들이 '법' 작업을 AI 에이전트로 할 때 설치하는 MCP 의 대표 중 하나가 된다 (← `OBJECTIVE.md`)
- **horizon**: 함정 없음 (← `plans/horizons/trap-free.md`)
- **milestone**: TF1 — 골드 4파일을 형식 중립 단일 코퍼스로 통합. milestone 규모 근거: 독립 step 3개
  (라벨 규약 ADR / 스키마·마이그레이션 / 러너 2종 배선)이고, 이를 가로지르는 통합 검증(기존 수치 재현)이 있다.

## run 전 scope 결정 (확정)
- **결정**: 통합과 재현까지. 맥락 부착(90건)은 TF2 소관 — 여기서는 `context` 필드를 비워 둔 채
  스키마에 자리만 만든다.
- **non-goals**: 새 케이스 추가 금지 · 라벨 내용 변경은 규약 적용에 필요한 최소만 · `src/` 변경 0줄.
- **중단점(stop points)**: step 검증 PASS 후 커밋 / 재현 수치 미달이면 blocked 로 정지(통합 실패 판정) /
  라벨 규약이 새 사용자 결정을 부르면 decision_required 로 정지.
- **롤백/정리**: 구 4파일은 삭제하지 않고 `archive/bench/` 로 이동한다 — 재현 실패 시 러너 배선만
  되돌리면 원상 복귀한다.

## 스캐폴딩 결정
- source-of-truth: `bench/corpus.json` 단일 파일. 통합 후 `bench/golden*.json` 4개는 `archive/bench/` 로
  이동(삭제 아님 — 원본 대조가 가능해야 재현 검증이 성립한다).
- 검증: `bench/check-schema.ts` 확장(전건 스키마·필수 필드·중복 라벨 탐지) · `npm test` · 러너 2종
  재실행 수치 대조.
- 배포/운영: 배포 영향 없음 — `bench/` 는 MCP 런타임에 포함되지 않는다. `custom-mcps` 사본 재빌드
  불요이며 `git diff --stat src/` 0줄이 이를 보증한다.
- 데이터: 케이스 id 규칙 `<출처약어><순번>`(`v2-01`·`tax-01`·`ag-d01`). 통합 전 파일명을 `provenance`
  필드로 보존해 어느 세트에서 왔는지 영구 추적한다.
- 검토 후 제외: frontend·backend·design·관측 — 벤치 데이터 통합이라 해당 없음.

## 결정 로그
- D-TF1-1 본법 vs 시행령 정답 라벨 규약 — 선택지 ① 항상 본법 ② 항상 구체 요건이 있는 쪽
  ③ 맥락이 묻는 것으로 가른다(위임 *근거*를 물으면 본법, 구체 *요건·한도·비율*을 물으면 시행령).
  추천 ③ — 실제 사용자 질문이 둘 다 존재하고 ①②는 절반을 구조적으로 오답 처리한다.
  확정값: **③ 맥락이 묻는 것으로 가른다** (2026-07-23 사용자 확정)
- status: resolved

## Step 트리
- [ ] **step-1 — 라벨 규약 ADR**
  - Artifact: `docs/adr/<NNNN>-본법-시행령-정답-라벨-규약.md` + 규약대로 맞춘 라벨 수정분
  - Files: read `bench/golden*.json`·`evidence/bench/2026-07-23-ar4-holdout.md` / write `docs/adr/` 신규 1개·`bench/golden*.json`(라벨 교정분만)
  - Dependencies: 없음 (D-TF1-1 확정이 착수 조건)
  - Risk: 위험 (라벨을 규약대로 고치면 지금까지 통과하던 케이스가 오답으로 바뀔 수 있다)
  - Verify: 위임 지점 케이스 전수 목록과 케이스별 판정 근거가 ADR 에 표로 남는다 · `npm run bench:verify-labels` exit 0
  - Failure probe: 규약을 반대로 적용한 케이스 1건을 넣어 `bench:verify-labels` 가 exit 1 로 잡는지 확인
  - Commit: `docs(adr): 본법·시행령 정답 라벨 규약 확정`
- [ ] **step-2 — 스키마와 마이그레이션**
  - Artifact: `bench/corpus.json` · `bench/migrate-corpus.ts` · 확장된 `bench/check-schema.ts`
  - Files: read `bench/golden*.json`·`bench/check-schema.ts` / write `bench/corpus.json`·`bench/migrate-corpus.ts`·`bench/check-schema.ts`·`archive/bench/`(이동)
  - Dependencies: step-1
  - Risk: 기계적
  - Verify: distinct 124건 · 전건 `query` 보유 · 전건 `provenance` 보유 · 스키마 검증 exit 0 ·
    `golden`(v1) 40건이 `golden-v2` 로 대체됐음을 마이그레이션 로그가 명시
  - Failure probe: 필수 필드를 하나 뺀 케이스를 넣어 스키마 검증이 exit 1 로 거부하는지 확인
  - Commit: `feat(bench): 골드 4파일을 단일 코퍼스로 통합`
- [ ] **step-3 — 러너 2종 배선과 재현 검증**
  - Artifact: 코퍼스를 읽는 러너 2종 + `evidence/bench/2026-07-23-tf1-reproduction.md`
  - Files: read `bench/run.ts`·`bench/agentic-run.ts`·`bench/agentic-set.ts`·`bench/scoring.ts` / write 위 러너들·`evidence/bench/2026-07-23-tf1-reproduction.md`
  - Dependencies: step-2
  - Risk: 기계적
  - Verify: 범용 dev `recall@3` >= 88.0% · agentic dev `pass^3` >= 90% · `npm test` 전건 통과 ·
    `git diff --stat src/` 0줄
  - Failure probe: 구 파일로 돌린 값과 코퍼스로 돌린 값을 나란히 표로 대조 — 차이가 있으면 원인까지 기록한다
  - Commit: `feat(bench): 러너 2종을 코퍼스로 배선 + 재현 검증`

## 검증/DoD
- `bench/corpus.json` 단일 파일이 distinct 124건을 담고 스키마 검증 exit 0.
- 러너 2종이 통합 전 수치를 재현한다 — 범용 dev `recall@3` >= 88.0% · agentic dev `pass^3` >= 90%.
  **미달이면 통합 실패로 판정한다**(새 기준선으로 받아들이지 않는다).
- 본법/시행령 규약이 ADR 로 존재하고 해당 케이스 전수가 규약대로 라벨링됨.
- `npm test` 전건 통과 · `git diff --stat src/` 0줄.

## 수치 출처
- 범용 dev `recall@3` 88.0% — `npm run bench:golden` (통합 전 기준값, `evidence/bench/2026-07-23-ar4-holdout.md`)
- agentic dev `pass^3` 90% — `npx tsx bench/agentic-run.ts` 3회 블라인드 (`evidence/bench/2026-07-23-ar2-baseline.md`)
- distinct 124건 — `golden-v2` 40 + `golden-tax` 50 + `golden-tax-agentic` 34 (`golden` v1 40건은 v2 가 대체)

## finding 큐
- F1 일반 세트(`golden-v2` 40건) 위임 지점 확인은 step-2 마이그레이션에서 `delegated_to` 기계
  스캔으로 처리한다 — 세법 84건은 step-1 에서 본문 확인으로 전수 완료.
- F2 **step 순서 재정렬 제안**: 라벨 변경을 step-3(재현 검증) *뒤*로 옮긴다. 라벨을 먼저 바꾸면
  기준선 수치가 움직여 "재현 실패"인지 "규약 효과"인지 구분 불가가 된다. 근거·순서는 ADR 0001
  "라벨 적용 시점" 절. 사용자 확인 필요(승인된 plan 의 step 경계 변경).

## 진행 로그 (append-only)
- 2026-07-23 plan 작성.
- 2026-07-23 step-1 규약 확정 — ADR 0001. 세법 84건 본문 확인, 라벨 변경 대상 4건 식별.
  라벨 *적용*은 F2 사유로 보류(step-3 이후 제안).
