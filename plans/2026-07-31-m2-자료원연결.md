# PLAN — M2 · 자료원 연결 (M1 처분 '연결 대상' 판정분)

> 생성: 2026-07-31 · 산출물: changeset(tooling) · scope: M1 처분표가 "연결 대상"으로 판정한
> 자료원을 기존 어댑터 패턴으로 연결
> 소비 리서치: `research/2026-07-31-세법완성-자료원지도.md` · M1 처분표 ADR

Status: approved (2026-07-31 사용자 승인 — M1→M2·M3→M4 연쇄 일괄)
- execution mode: continuous

## 위계
- **북극성**: 한국 사람들이 '법' 작업을 AI 에이전트로 할 때 설치하는 MCP 의 대표 중 하나가 된다 (← `CLAUDE.md` 「북극성」)
- **목표(연쇄)**: 세법 완성 — 자료원 완결 축. 연쇄 = M1→**M2**·M3→M4.
- **milestone**: M2 — 세무 근거 유형 지도에서 "연결 대상" 판정을 받은 자료원 전부에 도구 경로를 만든다.
  milestone 규모 근거: 독립 step 3개(법령계 연결 / 해석계 연결 / 예규 본문 도달) + 통합 검증
  (처분표의 "연결 대상" 행 전부가 실 MCP E2E 로 조회됨).

## 왜
북극성의 "함정 없음" 축 — 세무 질문의 근거 사슬 어느 고리를 물어도 도구 경로가 있어야 한다.
현재 확실한 공백: 조세조약(국제거래 과세의 1차 근거인데 경로 없음), 별표·서식(세율표 97건 실측),
신구법 비교, 그리고 기본통칙·집행기준(경로 존재 자체를 M1 이 판정).

## run 전 scope 결정 (확정)
- **결정**: M1 처분표의 "연결 대상" 행 전부. 연결 방식은 기존 `SOURCE_DESCRIPTORS` 패턴
  (`search_legal_source`/`get_legal_source` 의 source enum 확장) — 도구 개수를 늘리지 않는다.
- **non-goals**: 새 최상위 도구 추가 · 스크래핑(M1 D1 처분 기준) · 랭킹/검색 로직 변경 ·
  결함 수리(M3 소관) · HWP/PDF 본문 파싱 엔진 제작(별표는 메타+링크 수준까지).
- **중단점(stop points)**: 상류 응답이 M1 프로브와 다르면 blocked / 처분 기준 밖 선택이 필요하면 decision_required / 약관·시크릿 소지는 risk_gate 로 정지.
  (부연: "상류 응답이 다름" = M1 이 "연결 대상" 판정한 target 이 구현 시점에 다른 스키마·빈 응답을
  주는 경우. "처분 기준 밖" = 예: 본문 파싱 비용이 예상을 크게 초과해 D2 범위를 넘는 경우.)
- **롤백/정리**: descriptor 단위 추가라 자료원별로 독립 되돌림 가능. 데이터 잔재 없음.

## 스캐폴딩 결정
- source-of-truth: `src/providers/source-adapter.ts` 의 `SOURCE_DESCRIPTORS` 가 자료원 등록 정본.
  구속력 등급(`reference_only`/`adjudication` 류) 표기는 기존 규약(`research/2026-07-21-tax-domain-tax-practice-sources.md` §1)을 따른다.
- 검증: 자료원별 실 API 통합 테스트 + `npm test` 전건 + 실 MCP 클라이언트 E2E(응답 원문 증거).
  신규 테스트 파일명은 `test/m2-*` 접두로 통일 — M3 와 병렬 진행 시 충돌 방지.
- 배포/운영: `src/` 변경 — push → `custom-mcps/law-mcp` `git pull && npm run build` →
  MCP 서버 재시작(사용자). 재시작 부채를 완료 보고에 명시.
- 검토 후 제외: frontend·design — MCP 서버라 해당 없음.

## 결정 로그
- D1 — 연결 범위는 M1 처분표를 따른다(계획 단계 재질문 없음). 처분 기준 자체는 M1 결정 로그
  D1 에서 사용자 확정.
  - status: resolved
- D2 — 별표·서식(`licbyl`)은 메타데이터+원문링크 수준까지만 연결한다. HWP/PDF 본문 파싱은
  비용 대비 가치가 미검증이라 이 연쇄에서 하지 않는다 — 리허설(M4)이 부족을 드러내면 후보 적재.
  - status: resolved (2026-07-31 승인 제시에 포함해 사용자 확정)
- status: resolved

## Step 트리
- [x] **step-1 — 법령계 자료원 연결 (조세조약·별표·신구법 등)**
  - Artifact: M1 이 "연결 대상" 판정한 법령계 target(후보: `trty`·`licbyl`·`oldAndNew`·`lsStmd`) 의
    descriptor + 통합 테스트. M1 이 "공백/보류" 판정한 후보는 skip 하고 changeset README 에 사유 기록.
  - Files: read M1 처분표 ADR·`src/providers/source-adapter.ts` / write `src/providers/source-adapter.ts`·
    `src/index.ts`(source enum)·`test/` 통합 테스트
  - Dependencies: 없음
  - 착수 전제(milestone 순서는 ROADMAP marker 소유): M1 처분표 ADR 이 존재해야 착수 — 없으면 blocked
  - Risk: 위험 (상류 응답 스키마가 target 마다 다름)
  - Verify: 연결한 각 target 이 세법 실질 질의(예: 조세조약 "한미 조세조약"·별표 "소득세법 세율표")로
    실 API 결과를 반환 · `npm test` 전건
  - Failure probe: 존재하지 않는 검색어로 빈 결과가 에러가 아닌 "0건+안내"로 오는지 · 빈 응답 시
    어순 재확인 절차를 거쳤는지
  - Commit: `feat(m2): 법령계 자료원 연결 — M1 처분표 반영`
- [x] **step-2 — 해석계 자료원 연결 (기본통칙·집행기준 — M1 이 경로를 찾은 경우)**
  - Artifact: M1 이 확인한 경로로 기본통칙·집행기준 descriptor + 통합 테스트. 경로 없음 판정이면
    skip 하고 "공백+이유" 를 changeset README 와 처분표 ADR 에 남긴다(이 경우에도 step 은 판정
    기록으로 닫힌다).
  - Files: read M1 처분표 ADR·`src/providers/source-adapter.ts` / write `src/providers/source-adapter.ts`·`test/`
  - Dependencies: 없음
  - Risk: 위험 (경로 존재가 M1 결과 종속)
  - Verify: 연결 시 — 기본통칙 실질 질의(예: "부당행위계산 부인 기본통칙")가 실 결과 반환 ·
    구속력 등급이 `reference_only` 계열로 표기됨 · `npm test` 전건. 미연결 시 — 공백 기록이
    ADR·README 에 존재
  - Failure probe: 연결 시 법규성 없는 자료임을 알리는 단서(등급·주의 문구)가 응답에 실리는지 확인
  - Commit: `feat(m2): 해석계 자료원 연결 또는 공백 확정 — M1 처분표 반영`
- [ ] **step-3 — 예규 본문 도달 보강**
  - Artifact: `ntsExpc` 결과에서 에이전트가 본문에 실제로 닿는 경로(원문링크 해석 또는 상류 전문
    엔드포인트) 확인·보강 + 통합 테스트. M1 프로브가 "링크로 도달 가능" 판정이면 응답 안내 보강까지만.
  - Files: read `src/providers/source-adapter.ts:454-514`·M1 프로브 기록 / write `src/providers/source-adapter.ts`·`test/`
  - Dependencies: 없음
  - Risk: 기계적 (기존 descriptor 보강)
  - Verify: 예규 검색→본문 확인이 실 MCP 세션에서 한 체인으로 됨(응답 원문 증거) · `npm test` 전건
  - Failure probe: 원문링크가 로그인·세션 요구로 막히는 경우를 실측하고, 막히면 그 사실을 응답
    warning 으로 소비층에 알리는지 확인
  - Commit: `feat(m2): 예규 본문 도달 경로 보강`

## 검증/DoD
- M1 처분표의 "연결 대상" 행 전부가 연결되거나, 구현 불가 사유가 처분표 ADR 에 반영돼 행이
  "공백+이유" 로 재판정된다 — 판정 없는 행 잔존 = M2 미완료.
- 연결된 자료원 전부 실 MCP 클라이언트 E2E 응답 원문이 evidence 로 남는다.
- `npm test` 전건 · 배포 사본 build + dist 스모크 · 재시작 부채 명시.

## finding 큐
- (실행 중 발견분 append)

## 진행 로그 (append-only)
- 2026-07-31 plan 작성.
- 2026-07-31 step-1 완료 — trty·oldAndNew·licbyl 연결(어댑터 listSections 신규), 실 API E2E 그린,
  npm test 325/325. step-2 완료 — M1 판정(공백)에 따라 구현 없이 판정 기록으로 닫음(changeset §step-2).
